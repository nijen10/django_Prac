name: First django project

on:
  pull_request:
  types:
      - closed
jobs:
  build:
    if: github.event.pull_request.merged == true
    runs-on: devops1

    steps:
    - name: My first Github Action
      run: |
        echo "this is new pipeline"
        hostname
        ip a
        ls -al
        mkdir nijen
        ls -al
        pwd
    - name: git actions
      uses: actions/checkout@v4    

    - name: my first githuba actions
      run: |
      echo " code was merged successfully - running pipeline."
  # test:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #   - name: my test
  #     run: |
  #       echo "I am testing phase"
  # release:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: my release 
  #     run: echo " i am release"
  # deploy:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: my deploy
  #     run: echo "i am deploy phase"
